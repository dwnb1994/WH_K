# WH_K

Warehouse project workspace.

Useful copied references from the original `WH` folder:

- `docs/from-wh` - original WH documents, wireframes, architecture export, and `Test SKU.xlsx`.
- `docs/legacy-supabase` - old Supabase migrations/provider kept only for reference.
- `docs/legacy-web` - old dashboard/Next config kept only for reference.

Current app source lives in `apps/`, `packages/`, `trc/`, and `uploads/`.
