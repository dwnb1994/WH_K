# Legacy Web Reference

These files were copied from `WH/warehouse-app/apps/web` for reference only.

`WH_K` now uses:

- `apps/web/next.config.mjs`
- `apps/web/app/(shell)/dashboard/page.tsx`

The legacy dashboard page is kept here so old KPI/chart ideas do not need to be recreated if they become useful again.
