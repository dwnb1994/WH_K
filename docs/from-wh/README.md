# Files Copied From WH

Reference material copied from the original `WH` folder so the `WH_K` project can continue without recreating these assets.

Included:

- Original warehouse UI/wireframe HTML exports
- Original system architecture and Supabase notes
- `Test SKU.xlsx` sample data
- Original thumbnail asset

Runtime source code for `WH_K` remains at the project root under `apps/`, `packages/`, `trc/`, and `uploads/`.
